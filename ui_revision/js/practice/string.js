// now we are working with the string on js and functions on strings
// the  comment in js is like // and /* */ we use this for comments
// now string function
let str="hello world"
console.log(str)// we get the string values 
// to find the lenght of the string in js we use the length 
console.log(str.length)// so the length of the string is 11
// to find the index of the string we s[0]== the value
console.log(str[0])// h
console.log(str[1]=12)
console.log(str.lastIndexOf("l"))
console.log(str.indexOf("l"));
console.log(str.slice(0,5))
console.log(str.toUpperCase())
console.log(str.toLowerCase())
let str1="  hello prshanth     "
console.log(str1.trim())
//we are using the split()
// it will split the string in to the array of the string
let str2="hello world in js"
console.log(str2.split(" "))// ['hello', 'world', 'in', 'js']
// we  read in doc to understand in documention
